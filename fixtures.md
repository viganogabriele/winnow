# Fixture repositories

This file records the repositories used to verify winnow.

Fixtures live alongside this repository, not inside it. None are registered during Phase 0; they will
be added before the phases that execute winnow against real projects.
