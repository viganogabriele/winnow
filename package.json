{
	"name": "winnow",
	"private": true,
	"version": "0.0.1",
	"description": "The QA control plane GitHub doesn't give you: a reusable Actions workflow plus a self-hosted dashboard, assembled from open source tools.",
	"license": "Apache-2.0",
	"type": "module",
	"engines": {
		"node": ">=22"
	},
	"scripts": {
		"test": "vitest run --passWithNoTests"
	},
	"devEngines": {
		"packageManager": {
			"name": "pnpm",
			"version": "^11.21.0",
			"onFail": "download"
		}
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"tsx": "^4.23.12",
		"typescript": "^7.0.2",
		"vitest": "^4.1.10"
	}
}
